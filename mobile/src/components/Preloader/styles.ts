import { StyleSheet } from "react-native";

export const styles: Dictionary = StyleSheet.create({
  container: {
    alignItems: "center",
    marginTop: 25,
  },
  loadingGif: {
    height: 100,
    resizeMode: "stretch",
    width: 100,
  },
});
